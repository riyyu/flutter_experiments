import 'package:flutter/material.dart';

class GetResultPage extends StatelessWidget {
  const GetResultPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('t'),
      ),
    );
  }
}
