import 'package:flutter/material.dart';

class NewNavigationSeconPage extends StatelessWidget {
  const NewNavigationSeconPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('This is second page'),
      ),
    );
  }
}
